type Props = {
    userId: string,
    affiliate: boolean,
    stripeId?: string
}
// work in progress

export default function PaymentForm({userId, affiliate, stripeId}: Props) {
    return <div>

    </div>
}