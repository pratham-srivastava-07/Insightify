type SearchProps = {
    className?: string,
    inputStyle?: string,
    placeHolder?: string,
    searchType?: string
    iconStyle?: string,
    glass?: boolean
}

export default function Search({className, inputStyle, placeHolder, searchType, iconStyle, glass}: SearchProps) {
    
}